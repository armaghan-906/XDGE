node_modules
dist
.DS_Store
*.log
.vite
.vercel
.env
.env.local

public/assets/videos/Footages/

# Heavy source/scratch image dumps (optimized WebPs are committed instead)
public/assets/Images for Carousel 2/
public/assets/picss/
# Same deal: these are the 6000px PNG originals (296MB) behind the committed
# WebPs in the same folder. Nothing references them — the site loads only the
# .webp — so they were 296MB of every deploy payload for nothing. Kept on disk
# locally as the editable originals; the .webp siblings stay tracked.
public/assets/ALL NEW IMAGES/*.png
.env*
